import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'ionic.prograhibridaunidadii',
  appName: 'prograhibridaunidadii',
  webDir: 'www'
};

export default config;
